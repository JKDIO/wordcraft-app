diff --git a/app/admin_check.mjs b/app/admin_check.mjs
index f269ced..31b3dff 100644
--- a/app/admin_check.mjs
+++ b/app/admin_check.mjs
@@ -468,2 +468,8 @@ console.log('── ⑭-b ★v1.4.43 신규 봉인 (C2 · C4 · N1 · N2)★ ─
   ok(/flash-listen/.test(rm), '★복습 카드 앞면에 다시 듣기 버튼이 있다 (N2)')
+  // ★v1.4.45 (N2 봉합의 봉합 · 2026-08-18 격리 실기 검증)★ 렌더러만 고치면 소용없다 —
+  //   `W:CMD:*`/`W:ECHO:*` 카드는 모듈 콘텐츠가 아니라 listening 에서 나오므로,
+  //   backsMap 을 모듈에서만 채우면 **정작 그 17장에만 음성이 안 붙는다.** 공급 쪽을 잠근다.
+  const appx = src('src/App.tsx')
+  ok(/loadListening\(\)/.test(appx) && /W:CMD:\$\{/.test(appx) && /W:ECHO:\$\{/.test(appx),
+    '★backsMap이 지령·에코 카드(W:CMD/W:ECHO)의 음성을 실제로 공급한다 (N2 — 렌더러가 아니라 데이터)')
   // ★v1.4.43-b (AUDIT 지적)★ 실제 하루 최대는 cap + RESPAWN_EXTRA 다. 화면이 "60장까지만"이라고
diff --git a/app/src/App.tsx b/app/src/App.tsx
index fb00963..b8edd3b 100644
--- a/app/src/App.tsx
+++ b/app/src/App.tsx
@@ -25,3 +25,3 @@ import { XP } from './lib/xp'
 import { db, consumeAuthRedirect, getAuthUser, myDeviceLearner, ensureAnonSession, bindLegacyDevice, authKind, type Learner } from './lib/supabase'
-import { DIAG_ORDER, moduleOrder, loadModule } from './lib/content'
+import { DIAG_ORDER, moduleOrder, loadModule, loadListening } from './lib/content'
 import { computeEarnedBadges } from './lib/badges'
@@ -281,2 +281,27 @@ export default function App() {
 
+  /* ★v1.4.45 (N2 봉합의 봉합) — 2026-08-18 격리 실기 검증에서 잡혔다★
+     지령·에코 오답 카드(`W:CMD:*` / `W:ECHO:*`)는 **모듈 콘텐츠가 아니라 `listening`에서 나온다**
+     (`onModuleEvent`가 `W:${moduleId}:${question_id}` 로 시드한다).
+     그런데 위 effect는 `moduleOrder()`의 모듈 review_cards만 훑으므로, 그 카드들은 backsMap에
+     **항목이 아예 없다** → `meta?.tts`가 undefined → v1.4.43이 넣은 앞면 자동재생·🔊 버튼이
+     **정작 그 카드에만 안 걸렸다.** 앞면이 「본부에서 긴급 지령! 잘 들어봐」 같은 범용 문구인
+     바로 그 17장이다 — 소리가 없으면 문제 자체가 존재하지 않는다.
+     화면(렌더러)은 옳았고 **데이터 공급이 빠져 있었다**(L51 계열). 여기서 채운다. */
+  useEffect(() => {
+    loadListening().then(c => {
+      setBacksMap(prev => {
+        const nx = { ...prev }
+        for (const it of c.commands || []) {
+          nx[`W:CMD:${it.id}`] = { back: it.choices?.[it.answer_idx] ?? '', tts: it.tts || it.play || null }
+        }
+        for (const st of c.echo_sets || []) {
+          for (const it of st.items || []) {
+            nx[`W:ECHO:${it.id}`] = { back: it.choices?.[it.answer_idx] ?? '', tts: it.tts || it.play || null }
+          }
+        }
+        return nx
+      })
+    }).catch(() => { /* 리스닝 콘텐츠 실패 — 복습은 계속된다 */ })
+  }, [])
+
   // ── 이벤트 핸들러 ──
diff --git a/app/src/lib/version.ts b/app/src/lib/version.ts
index d704ef9..f310b57 100644
--- a/app/src/lib/version.ts
+++ b/app/src/lib/version.ts
@@ -2,3 +2,3 @@
 // 배포 시 반드시 APP_VERSION과 dist/version.json의 version을 함께 올릴 것 (RELEASE_LOG 규칙)
-export const APP_VERSION = '1.4.44'
+export const APP_VERSION = '1.4.45'
 
diff --git a/version.json b/version.json
index ea66a45..2b5466f 100644
--- a/version.json
+++ b/version.json
@@ -1 +1 @@
-{"version": "1.4.44", "date": "2026-08-18", "notes": "⛏️ 복습 규칙이 정직해졌어! 「헷갈려」를 눌러도 오늘 몫이 늘지 않아. 그리고 지령 카드는 이제 소리부터 들려줘 🔊", "vocab_ready": true, "worlds_ready": true}
+{"version": "1.4.45", "date": "2026-08-18", "notes": "⛏️ 복습 규칙이 정직해졌어! 「헷갈려」를 눌러도 오늘 몫이 늘지 않아. 그리고 지령·에코 카드도 이제 소리부터 들려줘 🔊", "vocab_ready": true, "worlds_ready": true}

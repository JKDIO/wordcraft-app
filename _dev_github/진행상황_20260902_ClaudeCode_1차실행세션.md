# 진행상황 — Claude Code 1차 실행 세션 (2026-09-02)

> 기획 세션(claude.ai Cowork) → 코딩·배포 단계로 넘어온 첫 실행 세션.
> **이 세션은 배포하지 않았다.** 배포 전 단계까지의 실측·봉합·검증 기록이다.

---

## 0. 진실 3종 대조 (착수 자가검증)

| 출처 | 값 | 확인 방법 |
|---|---|---|
| 최신 `RELEASE_v*.md` | 1.4.45 | `RELEASE_v1.4.46.md` **없음** |
| 라이브 `version.json` (`?cb=`) | **1.4.46** | HTTP 200 |
| 로컬 소스 `APP_VERSION` | **1.4.46** | `src_v1.4.46` · `src_v1.4.47` 둘 다 |

- 로컬 `wordcraft-app/main.js` sha256 == 라이브 `main.js` sha256 (`03a45ec1e65327e8…`) → **비트 동일**(L32).
- 라이브 번들 grep: `write-grade` 0건 · `writeFree` 0건 (대조군 `summon` 1건) → **쓰기 기능은 라이브에 없다.**

**확정: 실제 현재 버전 = v1.4.46.** 문서(1.4.45)가 낡았고 진실 3종이 맞다.

### 문서 결손 (실측)
- `RELEASE_v1.4.43/44/46.md` 없음 (43·44 내용은 45 문서 안에 있음, **46은 어디에도 요약 없음**)
- `00_START_HERE.md` 스탬프 = v1.4.30 / `00_START_HERE_스탬프_최신.md` = v1.4.42 (둘 다 낡음, 본문 §9~12는 08-29 갱신됨)
- **교훈 L45 이후 문서가 이 폴더에 없다.** 두 LESSONS 파일은 L44에서 끝나는데 코드는 L47·L51·L56·L61·L67을 인용한다.
- `연동계약_CONTRACT_v1.7.md` 는 **§15에서 끝난다 — §16이 없다.** `write-grade/index.ts` 와 `writeGrade.ts` 가 "CONTRACT §16"을 인용하지만 그 절은 아직 쓰이지 않았다.
  (★근거 보강 2026-09-02 FA-7★ 최초 확인에 쓴 grep 패턴이 0건을 반환해 **근거 없이 단정한 상태였다.** 패턴을 고쳐 재확인: 절 목록이 `## 1. 데이터 계약` ~ `## 15. 보상 로드맵 계약` 으로 끝나고 `§16`·`## 16` 검색 0건.)
- **로컬은 git 저장소가 아니다.** `WordCraft/`·`wordcraft-app/`·`_dev_github/` 어디에도 `.git` 없음 → GitHub 반영은 GitHub MCP 경유. 대상 owner/repo는 **아직 확인 못 함**.

---

## 1. 환경 복구 (Dio님 승인 후 실행)

- `CLAUDE.md` (Foundation 씨앗 V5.4.7) 를 `Projects/WordCraft/` 에 심음. `08_DOMAIN` 슬롯은 비워 둠(Dio님 입력 대기).
- `npm install -g bun` → **bun 1.4.0**. 이전까지 이 PC에는 bun이 없었고, `verify.sh`·`build.sh`·검사 15종이 전부 `bun build` 에 의존하므로 **노트북에서는 검사를 한 번도 돌린 적이 없었다.**
- `bun install` (react 19.2.6 / react-dom) + `npm install --no-save typescript @types/react @types/react-dom` (package.json 무변경 — 컨테이너에 전역으로 있던 것들)
- 롤백: `npm uninstall -g bun` · `rm -rf src_v1.4.47/app/node_modules` · `rm Projects/WordCraft/CLAUDE.md`

---

## 2. 앞 세션(2026-08-30) 산출물 검수 — `src_v1.4.47`

폴더 이름은 1.4.47이지만 **`APP_VERSION`은 아직 1.4.46이고 릴리스 문서도 배포도 없다.** 미완 트리다.

신규: `src/engine/WriteFree.tsx` · `src/lib/writeGrade.ts` · `supabase/functions/write-grade/index.ts` ·
`write_check.mjs` · `public/css.d/43-write-free.css`
수정(전부 `.bak_writefree_20260830` 백업 동반): `StepRunner.tsx` · `content.ts` · `supabase.ts` · `xp.ts` · `AdminPage.tsx` · `verify.sh`

### 검사 실행 결과 — **전 항목 통과**
`TSC=./node_modules/.bin/tsc BUN_BIN=bun bash verify.sh` → ① 타입검사 ~ ⑬ 확장월드 **13종 + tsc 전부 PASS**
(⑭ 버전 3자 대조는 `dist` 미빌드로 "빌드 후 다시 실행할 것" 출력 — 실패 아님)

`write_check.mjs` 단독 → 통과. 단 마지막 줄이 **"자유 작문 문항 0개"** 를 정직하게 보고한다.

### 서버 측 실측
- Supabase 프로젝트 `wordcraft-yehan`(`gbynvzxgbpmoqdsriowz`)
- 테이블 `write_grade_logs` **이미 생성됨** (RLS on, 0행)
- Edge Function `write-grade` **이미 배포됨** (version 2, ACTIVE, verify_jwt true)
- ⚠️ `{"probe":true}` 호출 → `{"openrouter_key":"없음"}` → **D47-4**

---

## 3. 채점기 실측 검증 (§C-2 재현 + §C-4 미검증 항목 신규)

OpenRouter T2 `deepseek/deepseek-v4-pro` 실호출 **총 9건** — 봉합 전 7건(설계 문서 §C-2 **5샘플 전부** + 한국어 신규 2건) + 봉합 후 재실측 2건.
★정정(FA-5)★ Dio님께 드린 최초 보고에는 "10건(8+2)"이라고 적었다. 도구 호출 로그 재집계 결과 **9건(7+2)** 이 맞다.
설계 문서 §C-4가 "**특히 한국어 섞어쓰기는 아직 테스트하지 않았다**"고 스스로 적어 둔 구멍을 실제로 재봤다.

**결과: 결함 3건 발견 → 봉합 → 재실측 교정 확인.** 상세는 `결함대장_DEFECT_LEDGER_v1.md` §4 (D47-1~D47-5).

가장 큰 것 (D47-1, 치명): **한국어로만 쓴 답에 +15 XP.** 영작문 과제인데 영어를 한 글자도 안 써도 만점이었고
`confidence:high · parent_queue:불필요` 라 아빠 화면에도 아무 표시가 남지 않았다.

### 봉합 방식 — §C-3 원문 무수정 원칙 유지
Dio님 지시("§C-3 최종 시스템 프롬프트를 그대로 쓴다")를 지키기 위해 **원문은 한 글자도 고치지 않고**
앞 세션이 만든 `[실측 보강]` A~D 아래에 `[실측 보강 2]` **E~H를 덧붙였다.**

| 절 | 내용 |
|---|---|
| E | 영어 단어 0개(한국어만) → xp 0. 단 "무성의"가 아니라 **"언어만 달랐다"**로 말하고, 내용을 먼저 인정한 뒤 영어로 한 문장 유도 |
| F | 한영 혼용(영어 1단어 이상) → xp 15 유지. 단 **영어로 쓴 부분만 인용**해 칭찬 · confidence `medium` · parent_queue 등록 |
| G | **아이가 쓰지 않은 단어를 인용 금지** (실측: 쓴 적 없는 `won`을 인용해 고쳐 주려 했다) |
| H | 말투 반말 통일, 교사 화자 금지 (실측: 한 건만 "선생님도 다 기뻤어요") |

백업: `supabase/functions/write-grade/index.ts.bak_D47-1_korean_20260902`

**재실측 (봉합 후, 같은 2건)**
- 한국어만 → `xp:0` + `"친구랑 축구해서 이긴 얘기 정말 재밌다! 이번엔 영어로도 한 문장만 써볼까? 'I played soccer'처럼 짧아도 괜찮아!"` ✅
- 한영 혼용 → `xp:15` · `confidence:medium` · `parent_queue:"한국어 섞어쓰기 — 참고용"` · 칭찬은 영어 부분만 인용 ✅

---

## 4. 기사 갤러리 오버레이 — 파일럿 2장 (본 생산 아님)

`저작규격_기사갤러리_오버레이파이프라인_v1.md`(v1.4)의 §11 결과물이 로컬에 저장돼 있지 않아(fal CDN 임시 URL) **재생성부터 했다.**

| 산출물 | 파일 | 결과 |
|---|---|---|
| 베이스 바디 | `UIUX_MVP_목업/_fal_test/hero_base_v1_20260902.png` (896×1200) | 뿔 없음 · 로고 없음 · 페인터리 · 전신 정면 · 회색 배경 **PASS** |
| 파일럿 오버레이 (helm t3) | `_fal_test/helm_t3_pilot_20260902.png` | 뿔 없음 · 얼굴 보임 · 깃털 크레스트 **PASS** |

모델·호출법은 문서 지시대로 `run_model(model="fal-ai/nano-banana-pro/edit", input={"prompt":…, "image_urls":[…]})`.
negative는 HERO_SPEC §5 문구(`horns, horned helmet, devil horns, demon horns`)를 프롬프트 안 금지절로 그대로 포함.

### ★핵심 실측 — 레이어 추출이 실제로 된다★
베이스 ↔ 헬멧본 픽셀 차분(896×1200 = 1,075,200px):

```
threshold  48 : 변경 16,924px (1.57%)
변경 바운딩박스: x 370~515 · y 13~257   ← 정확히 투구 영역
머리 영역(y<300) 밖 변경: 0px (0.000%)
```

**모델이 나머지 캔버스를 건드리지 않는다**는 것이 수치로 확인됐다. 알파 = `clip((diff-24)*4, 0, 255)` 로
투명 레이어를 뽑아 자홍색 배경에 합성한 결과 **투구만 남았다**(`helm_t3_layer_on_magenta.png`).
→ 24장 본 생산의 기술적 전제가 성립한다.

**단, 알파가 아직 거칠다** — 투구 내부에서 베이스와 색이 비슷한 부분(피부·머리색과 겹치는 곳)이 반투명으로 뚫린다.
본 생산 전에 알파 후처리(내부 채움/모폴로지)를 정해야 한다. 이건 §1~§7에 규정돼 있을 가능성이 높다(아래 블로커).

---

## 5. 남은 블로커 (Dio님 조치 필요)

| # | 블로커 | 왜 Claude가 못 하나 |
|---|---|---|
| **B1** | **`OPENROUTER_API_KEY` 가 Supabase Edge Function 시크릿에 없다** (D47-4) | 자격증명 취급 금지 + 키를 모른다. Dio님이 Supabase 대시보드 → Project Settings → Edge Functions → Secrets 에서 직접 등록. **등록 전에는 쓰기 기능을 예한이에게 열면 안 된다** — 모든 제출이 규칙 폴백으로 떨어져 고정 문구만 나간다 |
| **B2** | **오버레이 저작규격 §1~§7이 이 폴더에 없다** | 문서 자신이 "필요 시 Dio님께 v1.3 요청"이라고 적어 둠. 캔버스 규격(800×1200인지 896×1200인지)·슬롯/등급 정의·알파 후처리·파일명/저장 경로·앱 합성 방식이 전부 그 안에 있다. 없이 24장을 뽑으면 **규격을 내가 지어내는 것**이 된다 |
| **B3** | **GitHub 저장소 owner/repo 미확인** | 로컬이 git 저장소가 아니다. ★근거 보강 2026-09-02★ 최초에는 `00_START_HERE.md` **한 파일만** grep 했었다. 트리 전수 grep(`*.md/yml/json/sh/mjs`)으로 재확인 → 프로젝트 저장소 URL **0건**(node_modules의 서드파티 URL만 나옴). `.github/workflows/*.yml` 7종과 `_dev_github/ci/wc-build.yml` 도 열어 봤으나 owner/repo 문자열 없음. "기록 3종" 중 GitHub 반영 대상을 모른다 |
| **B4** | **자유 작문 문항을 어디에 넣을지가 미정** | `write_free` 스텝을 이미 클리어한 52개 모듈에 끼워 넣으면 아이의 진행 상태가 조용히 바뀐다(월드 7~10 숨김 구조가 경계하는 바로 그 사고). `worlds_ready` 처럼 **`writing_ready` 스위치 + 별도 배치**가 맞다고 보지만 이건 아이 경험 설계라 Dio님 결정 |
| B5 | 베이스 바디의 **얼굴이 원본보다 청년에 가깝다** | 예한이 아바타로 적절한 나이대인지는 Dio님 판단. 재생성으로 조정 가능 |

---

## 6. 배포 체크리스트 (아직 하나도 실행 안 함 — 이 세션은 배포하지 않았다)

1. [ ] B1 해결 (OpenRouter 키) → `{"probe":true}` 로 `"설정됨"` 확인
2. [ ] 패치된 `write-grade` Edge Function **재배포** (현재 라이브는 D47-1 봉합 **이전** v2다)
3. [ ] B4 결정 → 자유 작문 문항 저작 → `write_check` ⑦이 "0개"가 아니게 됨
4. [ ] `APP_VERSION` 1.4.46 → 1.4.47, `version.json` 동시 갱신
5. [ ] `bash verify.sh` → `bash build.sh` → `bash verify.sh`(⑭ 3자 대조 통과) → 렌더 스모크 → 배포
6. [ ] 라이브 SHA·번들 grep 대조 (`writeFree` 가 잡혀야 한다)
7. [ ] 기록 3종: `RELEASE_v1.4.47.md` + `00_START_HERE` 계열 갱신 + GitHub 반영(B3 필요)

---

## 7. 이번 세션 판정

| 항목 | 판정 | 근거 |
|---|---|---|
| 현재 버전 확정 | **PASS** | 진실 3종 + 번들 SHA 실측 |
| `src_v1.4.47` 검사 통과 | **PASS** | verify.sh 13종 + tsc, 노트북에서 실행 |
| 채점기 판정 품질 | **MORE_EVIDENCE** | 실호출 9건은 통계가 아니다. 예한이 실제 응답 0건. Edge Function 경유 end-to-end 채점은 **한 번도 성공한 적 없다**(B1) |
| 오버레이 본 생산 | **BLOCK** | B2 — 규격 §1~§7 없음. 파일럿 2장으로 기술적 성립은 확인했으나 규격은 못 만든다 |
| 읽기·쓰기 콘텐츠 확대 | **BLOCK** | B4 — 배치 설계 미결정 |
| UI/UX v6.1~6.3 실반영 | **미착수** | ★FA-10 4요소★ ①안 한 것: `진행상황_20260822_UIUX_MVP_v1.md` v6.1~6.3(관제실 톤 격상·오라 링4겹/파티클11·clip-path(ellipse) 초상화·왕관)의 `src_v1.4.47/app/src/screens` 실반영 ②원인: 이 사이클을 (a)채점기 봉합과 (b)오버레이 파일럿에 썼고, 남은 시간을 새 작업 착수 대신 블로커 보고에 배정했다 — 기술적 장애가 아니라 **내 배정 판단**이다 ③기한: 다음 사이클 최우선. B1·B2 답을 기다릴 필요가 없는 유일한 항목이라 **블로커와 무관하게 바로 시작한다** ④확인 방법: `AdminPage.tsx`·기사 초상화 컴포넌트에 `clip-path: ellipse` 존재 grep + `mask-image` 0건 grep(v6.2 원인 재발 감시) + 빌드 후 렌더 스모크 화면 대조 |
| 배포 | **미실행** | 위 체크리스트 7단계 중 0단계 |

---

## 8. 2026-09-03 — B1~B5 답신 반영 (2차 사이클)

| 블로커 | 결과 |
|---|---|
| **B1** | **해소.** Dio님이 키 등록 → e2e 실측 → **새 치명 결함 D47-6 발견·봉합·배포(Edge Function v5)**. D47-1·D47-2·D47-3·D47-4 해소 처리 |
| **B2** | **해소.** v1.3 유실 확정, **896×1200 정본 채택**. 오버레이 문서 **v2.0 §12 신규 정본** 작성 + 추출 도구 `app/tools/overlay_layer.py` 신설 + 파일럿 검증 |
| **B3** | **해소.** 저장소 = `https://github.com/JKDIO/wordcraft-app` |
| **B4** | 승인 접수(`writing_ready` 스위치 + 별도 배치). **코드 미착수** — 다음 사이클 |
| **B5** | 보류(Dio님). 24장 생산 후 재판단 |

### 이 사이클에서 실제로 한 것
1. 봉합된 `write-grade` 배포(v4) → e2e 실측 → **D47-6 발견** → 원인 규명 → 재봉합·배포(v5)
2. LEAN 게이트(300줄) 지적을 받아 **시스템 프롬프트를 `prompt.ts` 로 분리** — 프롬프트와 배관은 수명이 다르다
3. `app/tools/overlay_layer.py` 신설: 차분 → 노이즈 제거 → 구멍 메우기 → 알파 → **오염도 게이트**.
   멱등성·되돌림 게이트·오염도 게이트를 각각 일부러 깨뜨려 **실제로 우는 것까지 확인**(L27④)
4. 임시 진단 함수·테스트 학습자 **전량 정리** — `learners` 4명 복귀, `write_grade_logs` 0행,
   `answer_events` 6,366행 무변동(손대지 않았다)

### 다음 사이클 (B4 → 배포)
1. `writing_ready` 스위치 구현(`worlds_ready` 패턴 재사용) — **기존 52개 모듈 무접촉**
2. 자유 작문 문항 저작 → `write_check` ⑦ "0개" 해소
3. 읽기(문단독해)=`story`+`quiz` · 쓰기(빈칸)=`game:choice` 로 **스키마 변경 0** 저작
4. UI/UX v6.1~6.3 실반영 (§7 FA-10 4요소 참조)
5. 오버레이 24장 본 생산(§12 규격)
6. `APP_VERSION` 1.4.47 → 빌드 → verify → 배포 → 기록 3종(GitHub = JKDIO/wordcraft-app)

---

## 9. 2026-09-03 사이클 3 — B4·(c)·(d)·(b) 전부 이행, 배포 직전까지

이 사이클의 산출물은 **`RELEASE_v1.4.47.md` 가 정본**이다. 여기서는 결론만 적는다.

| 항목 | 결과 |
|---|---|
| B4 writing_ready | **완료** — 월드 10 이야기 서고. 기존 52모듈 진도·뱃지 무변경(reading_check ②가 강제) |
| (c) 읽기·쓰기 콘텐츠 | **완료** — 12모듈 · 지문 18 · 4지선다 72 · 자유작문 12 |
| (d) UI/UX v6 | **완료(이식 가능분)** — 관제실 톤·오라 4겹/파티클 11. 초상화 clip-path 는 **대상 없음**(프로덕션에 회화풍 초상화가 없다) → mask-image 금지 게이트로 대체 |
| (b) 오버레이 24장 | **완료** — 생성 24 · 레이어 추출 24 · 육안 O1~O4 통과. Storage 업로드는 미실행 |
| 빌드·검증 | **완료** — 검사 15종 + 타입검사 + 3자 대조 통과(exit 0) |
| 렌더 스모크 | **완료** — 브라우저 도구로 대체(Playwright 미설치) |
| 배포 | **미실행** — push 가 CI를 태워 라이브까지 나가므로 승인 대기 |

★이 사이클에서 **검사 자체의 결함 4건**을 잡았다(D47-11·12·14·15). 특히 verify.sh 가
**타입 검사 실패를 삼키고 "전 항목 통과"를 찍고 있었다** — 결함대장 §6을 반드시 읽을 것.

*작성: 2026-09-02 · 갱신: 2026-09-03(사이클 2·3) · Claude Code (Opus 5)*
